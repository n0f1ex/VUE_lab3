# VUE_lab3
## Были созданы две .html страницы для 6 и 7 работы соответственно
### Папка 6 - DOM API (lab3-6)
Была создана более сложная часть .html страницы, с разными шрифтами, экранированием и таблицей.

Далее идёт кликер, с сохранением данных в localStorage, для переноса результата между перезагрезками.

После идет калькулятор на базе обратной польской нотации, где символы помещаются в стек.
### Папка 7 - CSS (lab3-7)
"Более сложная" часть .html страницы была покрашена (цирк уехал, вывеска осталась);

для изменения ширины экрана был применен @media в .css, внутри которого были указаны размеры окна для смены отображаемого класса (юdesctop, .tablet, .phone);

CSS позиционирование круга было реалтзовано с помощью .addEventListener('mousemove'... который изменял позицию круга при перемещении мыши;

Страница с картинки получилось такой благодаря классам .css, позволяюзим настраивать размер/положение блоков с текстом
